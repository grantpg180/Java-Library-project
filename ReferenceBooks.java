package LibraryLab;

public class ReferenceBooks extends Books {
    public ReferenceBooks(String title, String genre, int pubYear, int price, int checkOutLimit, int stock){
        super(title, genre, pubYear, price, checkOutLimit, stock);
    } 
}